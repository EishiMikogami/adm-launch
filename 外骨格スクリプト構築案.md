# 認証プロセスにおける内骨格に「secure_process.sh」のコードを用いて、内骨格に、認証をパスした後の処理（外骨格)を追加していくことで、安全性を高いレベルで保ちつつ、任意の処理へ移行することを可能とする。

## 外骨格（以降、中核処理（認証後に行う、本編処理））の処理内容と、諸設定

設定ファイルおよびログファイルに関して

```bash

#!bin/bash/

ENC_DB=~/.config/.admin/".password.enc"
LOG_FILE=~/.config/.admin/".usr_access.log"


```
